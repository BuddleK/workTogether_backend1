<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NormalUsersMapper">

    <insert id="join" parameterType="com.wt.app.dto.NormalUsersDTO">
    INSERT INTO tbl_normal_users (
      users_number, normal_users_level
    ) VALUES (
      #{usersNumber}, #{normalUsersLevel}
    )
  </insert>

</mapper>